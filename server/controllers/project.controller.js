/**
 * controllers/project.controller.js — Project HTTP handlers.
 *
 * Business logic belongs in project.service.js. Handlers will be added when
 * project persistence and authentication are implemented.
 */

'use strict';

const projectController = {};

module.exports = projectController;
